<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Doner Place</title>
  <link rel="stylesheet" type="text/css" href="/css/site.css" />
</head>

<body>
  <div class="container">
    <header>
      <span class="logo">Doner Place</span>
      {{#if currentUser}}
      <form id="logout-form" action="/logout" method="POST"></form>
      <span class="greeting">Welcome, {{currentUser.username}}
        <a href="javascript:document.getElementById('logout-form').submit()">[Logout]</a>
      </span>
      {{else}}
      <span class="greeting">Welcome!</span>
      {{/if}}
      <ul class="menu">
        <li>
          <a class="button" href="/">Home</a>
        </li>
        {{#if currentUser}}
        <li>
          <a class="button" href="/orderStatus">Order Status</a>
        </li>
        {{else}}
        <li>
          <a class="button" href="/login">Login</a>
        </li>
        <li>
          <a class="button" href="/register">Register</a>
        </li>
        {{/if}}
      </ul>
    </header>
    {{#if globalErr}}
    <h2 class="error-msg">{{globalErr}}</h2>
    {{/if}} {{{body}}}
    <footer>Doner Place &copy; 2017</footer>
  </div>
</body>

</html>